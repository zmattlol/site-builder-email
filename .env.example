PORT=3000

# When true, emails are not sent but logged as preview.
TEST_MODE=true

# SMTP settings (required when TEST_MODE=false)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_user
SMTP_PASS=your_password
SMTP_FROM_NAME=Your Name
SMTP_FROM_EMAIL=you@example.com
